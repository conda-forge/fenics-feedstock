mpi:
  - 'mpich'
petsc:
  - '3.10.*'
numpy:
  - '1.15.*'
mpi4py:
  - '3.0.0'
pin_run_as_build:
  eigen:
    max_pin: x.x
  numpy:
    max_pin: x.x
  mpich:
    max_pin: x.x
  petsc:
    max_pin: x.x
  petsc4py:
    max_pin: x.x
  ptscotch:
    max_pin: x.x.x
  # pybind11 generation breaks on patch releases
  # at least between 2.2.3 and 2.2.4
  pybind11:
    max_pin: x.x.x
  slepc:
    max_pin: x.x
  slepc4py:
    max_pin: x.x
